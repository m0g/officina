---
layout: layout.njk
---
<div>
<div class="h-full min-h-full relative md:my-32">
  <div class="h-full flex min-h-full z-10 relative">
    <div class="h-full max-w-screen-xl mx-auto px-4 flex flex-col md:flex-row">
      <div class="md:w-1/2 md:mt-20 mb-10">
        <h1 class="font-bold italic text-6xl text-black leading-tight">
          {{ title }}
        </h1>
        <div class="text-sm md:text-base text-black my-4 space-y-3">
          {{ content | safe }}
        </div>
      </div>
      <div class="md:ml-16 md:w-1/3 relative">
        {% image image, "Artist residency at Officina Kreuzberg" %}
      </div>
    </div>
  </div>
  <div class="absolute -bottom-4 left-0 right-0">
    <div class="text-sm italic text-center">Scroll down</div>
    <div class="text-center text-2xl">&#9660;</div>
  </div>
</div>
<div class="bg-blue-custom pt-24">
  <div class="h-full max-w-screen-xl mx-auto px-4 flex flex-col">
    <h2 class="font-bold italic text-5xl text-salmon md:w-1/3 leading-tight">
      The residents
    </h2>
    <div class="flex flex-col mt-12">
      {%- for artist in collections.artist | sortByPosition | reverse -%}
        <div class="mb-16 max-w-screen-xl mx-auto flex flex-col md:flex-row">
          <div class="md:w-1/3 md:mr-10">
            {% image artist.data.image, artist.data.name %}
          </div>
          <div class="md:w-1/2">
            <h2 class="text-white font-plex text-salmon text-2xl font-bold">
              {{ artist.data.name }}
            </h2>
            <div class="text-white italic">{{ artist.data.period }}</div>
            <div class="text-white my-8 space-y-4 md:max-w-2xl">
              {{ artist.templateContent | safe }}
            </div>
            <div>
              <a
                target="blank"
                rel="noopener"
                href="{{ artist.data.website }}"
                class="text-salmon underline underline-offset-4 transition-colors duration-150 hover:text-white"
              >
                {{ artist.data.website}}
              </a>
            </div>
          </div>
        </div>
      {%- endfor -%}
    </div>
  </div>
</div>
